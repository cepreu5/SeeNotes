# Множество отворени бележки - Задачи

- `[x]` **HTML (index.html)**
  - `[x]` Преименуване на `#content-modal` на `#content-modal-template`.
  - `[x]` Замяна на вътрешните ID-та (напр. `#modal-body`, `#modal-board-name`, бутони) с класове.
  - `[x]` Скриване на шаблона по подразбиране (`display: none`).

- `[x]` **CSS (style.css)**
  - `[x]` Актуализиране на селекторите за модала (от `#` към `.`).
  - `[x]` Добавяне на стилове за динамично генерирани модали и z-index управление.

- `[/]` **Управление на модали (main.js - showModal)**
  - `[ ]` Създаване на логика за клониране на шаблона.
  - `[ ]` Генериране на уникални ID-та за всяка отворена бележка (напр. `note-modal-{gdid}`).
  - `[ ]` Проверка дали бележката е вече отворена (фокусиране вместо ново отваряне).
  - `[ ]` Закачане на събития към бутоните на конкретния клон, вместо глобално.
  - `[ ]` Каскадно отместване при отваряне на няколко прозореца.

- `[ ]` **Драг, Оразмеряване и Поведение (main.js)**
  - `[ ]` Увеличаване на z-index при клик върху даден модал (фокусиране).
  - `[ ]` Запомняне на размера: при оразмеряване да се запазва като `modalWidth` и `modalHeight` в localStorage, за да важи за следващите.
  - `[ ]` Обвързване на Drag логиката с конкретния прозорец.

- `[ ]` **Редактиране и Запис (main.js)**
  - `[ ]` Адаптиране на click-to-edit да работи в рамките на конкретния `.modal-body`.
  - `[ ]` Адаптиране на логиката за запис (`Ctrl+S`, бутона за запис) да взима контекста от активния (последния фокусиран) модал.
  - `[ ]` Промяна на `checkUnsavedChanges()` да работи с инстанция на модал.
  - `[ ]` Обхождане на всички отворени модали при глобална проверка за незапазени промени (напр. при фонов синк).

- `[ ]` **UI Добавки (main.js / index.html)**
  - `[ ]` Добавяне на опция "Затвори всички бележки" в плаващия бутон (popup менюто).

- `[ ]` **Синхронизация и Конфликти (main.js)**
  - `[ ]` Обхождане на всички отворени прозорци при проверка за конфликт с дошли от GDrive промени.
  - `[ ]` Live update (автоматично опресняване) на отворена бележка, ако няма локални промени.
